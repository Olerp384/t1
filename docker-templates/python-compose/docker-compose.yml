version: "3.9"

services:
  app:
    build:
      context: ..
      dockerfile: Dockerfile
      args:
        PYTHON_VERSION: 3.11
    env_file:
      - ../.env
    ports:
      - "${PORT:-8000}:8000"
    command: ["python", "main.py"]
